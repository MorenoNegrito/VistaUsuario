package com.example.vetapp_usuario.ui.theme

import androidx.compose.ui.graphics.Color

// Colores Principales - Azul Profesional
val PrimaryBlue = Color(0xFF2563EB) // Azul principal vibrante
val PrimaryBlueLight = Color(0xFF3B82F6) // Azul claro para hover
val PrimaryBlueDark = Color(0xFF1E40AF) // Azul oscuro para énfasis

// Colores de Fondo - Blanco y Grises Suaves
val BackgroundWhite = Color(0xFFFFFFFF)
val BackgroundLight = Color(0xFFF8FAFC) // Gris muy claro
val BackgroundGray = Color(0xFFF1F5F9) // Gris suave para cards

// Colores de Texto
val TextPrimary = Color(0xFF1E293B) // Casi negro para texto principal
val TextSecondary = Color(0xFF64748B) // Gris para texto secundario
val TextTertiary = Color(0xFF94A3B8) // Gris claro para hints

// Colores de Acento
val AccentGreen = Color(0xFF10B981) // Verde para éxito
val AccentOrange = Color(0xFFF59E0B) // Naranja para advertencias
val AccentRed = Color(0xFFEF4444) // Rojo para errores
val AccentPurple = Color(0xFF8B5CF6) // Púrpura para destacados

// Colores de Estado
val StatusPending = Color(0xFFFBBF24) // Amarillo para pendiente
val StatusConfirmed = Color(0xFF3B82F6) // Azul para confirmado
val StatusCompleted = Color(0xFF10B981) // Verde para completado
val StatusCancelled = Color(0xFFEF4444) // Rojo para cancelado

// Bordes y Divisores
val BorderLight = Color(0xFFE2E8F0)
val BorderMedium = Color(0xFFCBD5E1)
val DividerColor = Color(0xFFE2E8F0)

// Sombras y Overlays
val ShadowColor = Color(0x1A000000) // 10% negro
val OverlayDark = Color(0x80000000) // 50% negro para modales

// Colores Específicos de la App
val VetCardBackground = Color(0xFFFFFFFF)
val VetCardBorder = Color(0xFFE2E8F0)
val IconBackground = Color(0xFFEFF6FF) // Azul muy suave para fondos de íconos
val IconTint = Color(0xFF2563EB)